Installation:
	
# clone the repository
> git clone git://github.com/jmaicher/dotvim.git ~/.vim

# create symlinks
> ln -s ~/.vim/.vimrc ~/.vimrc

# init/update git submodules
> git submodule init && git submodule update
